apiVersion: 1
providers:
  - name: Grid Pulse Analytics
    orgId: 1
    folder: Grid Pulse Analytics
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
